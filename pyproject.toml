[project]
name = "efficient-on-device-ai"
version = "0.1.0"
description = "Integer-only LLM inference on edge: validation, smoothing, and edge benchmarking"
requires-python = ">=3.10"
dependencies = ["numpy>=1.24"]

[tool.setuptools.packages.find]
include = ["intops*", "validation*", "bench*"]
